# 03A · 数据字典（字段级明细）

> 本文件是 `03-数据库设计规范.md` 的配套明细，**每一张表建表后必须在此登记**。
> 变更流程见 `03` §四；枚举值定义见 `03` §三。
> 对应 migration：`server/database/migrations/2026_09_15_0000XX_*.php`
> 最后更新：2026-09-15

图例：`NN` = NOT NULL；`FK` = 逻辑外键（无物理外键，靠应用层保证）；`IDX` = 已建索引。

---

## 一、用户域（user_*）

### 1.1 user_accounts · 用户账号表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ 权限：客户端读写

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| uid | varchar(16) | NN | — | 用户展示 UID，注册时生成；`uk_user_accounts_uid` |
| mobile | varchar(20) | NN | '' | 手机号（不含国家码）；`uk_user_accounts_mobile`；**注销时改写为 `deleted_{id}_{mobile}` 释放占用** |
| mobile_country_code | varchar(8) | NN | '86' | 手机号国家码 |
| password | varchar(100) | NN | '' | bcrypt 哈希；纯微信登录用户为空串 |
| wx_mp_openid | varchar(64) | NN | '' | 微信小程序 openid；`idx_user_accounts_wx_mp_openid` |
| wx_unionid | varchar(64) | NN | '' | 微信开放平台 unionid |
| register_source | tinyint | NN | 1 | 注册来源 1=手机号 2=微信小程序 3=后台导入 |
| status | tinyint | NN | 1 | 1=正常 2=禁用 3=注销中 4=已注销；`idx_user_accounts_status` |
| last_login_at | datetime | 是 | NULL | 最后登录时间 |
| last_login_ip | varchar(45) | NN | '' | 最后登录 IP（兼容 IPv6） |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 1.2 user_profiles · 用户资料表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ 与 `user_accounts` 1:1

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id`；`uk_user_profiles_user_id` |
| nickname | varchar(32) | NN | '' | 昵称 |
| avatar | varchar(255) | NN | '' | 头像 object_key 或完整 URL |
| gender | tinyint | NN | 0 | 0=未知 1=男 2=女 |
| birthday | date | 是 | NULL | 生日 |
| province | varchar(32) | NN | '' | 省 |
| city | varchar(32) | NN | '' | 市 |
| exam_target | varchar(64) | NN | '' | 备考目标，如「一级建造师」 |
| bio | varchar(255) | NN | '' | 个性签名 |
| study_days | int unsigned | NN | 0 | 累计学习天数 |
| study_seconds | int unsigned | NN | 0 | 累计学习时长（秒） |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 1.3 user_members · 用户会员表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ 与 `user_accounts` 1:1

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id`；`uk_user_members_user_id` |
| level | tinyint | NN | 0 | 0=普通 1=月卡 2=季卡 3=年卡 4=永久 |
| status | tinyint | NN | 1 | 1=生效 2=已过期 3=已冻结；`idx_user_members_status` |
| started_at | datetime | 是 | NULL | 生效时间 |
| expired_at | datetime | 是 | NULL | 到期时间，永久会员为 NULL；`idx_user_members_expired_at`（定时任务扫描过期） |
| source_type | tinyint | NN | 1 | 1=购买 2=系统赠送 3=活动奖励 |
| last_order_id | bigint unsigned | NN | 0 | 最近关联订单 ID |
| ai_import_quota | int unsigned | NN | 0 | 剩余 AI 导题配额（份） |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 1.4 user_daily_stats · 用户每日学习统计表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ 学习空间数据来源

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id` |
| stat_date | date | NN | — | 统计日期；`uk_user_daily_stats_user_id_stat_date` |
| answer_count | int unsigned | NN | 0 | 当日作答题数 |
| right_count | int unsigned | NN | 0 | 当日答对题数 |
| wrong_count | int unsigned | NN | 0 | 当日答错题数 |
| practice_count | int unsigned | NN | 0 | 当日练习次数 |
| exam_count | int unsigned | NN | 0 | 当日考试次数 |
| duration_seconds | int unsigned | NN | 0 | 当日学习时长（秒） |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 1.5 user_wrong_questions · 用户错题表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ **移除错题用 `status` 而非软删**，保证同题不重复入本

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id` |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`uk_user_wrong_questions_user_id_question_id` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID |
| wrong_count | int unsigned | NN | 1 | 累计答错次数 |
| last_wrong_at | datetime | 是 | NULL | 最近答错时间；`idx_user_wrong_questions_user_id_last_wrong_at` |
| source_type | tinyint | NN | 1 | 1=练习 2=考试 3=错题重做 |
| last_answer | varchar(500) | NN | '' | 最近一次错误作答内容 |
| status | tinyint | NN | 1 | 1=在错题本 2=已移除 3=已掌握；`idx_user_wrong_questions_user_id_bank_id_status` |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 1.6 user_favorite_questions · 用户收藏题表

> migration: `2026_09_15_000001` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id` |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`uk_user_favorite_questions_user_id_question_id` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID |
| folder_name | varchar(32) | NN | '默认收藏夹' | 收藏夹名称 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段；`idx_user_favorite_questions_user_id_created_at` |

### 1.7 user_practice_records · 用户练习记录表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ 支持断点续练

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id` |
| bank_id | bigint unsigned | NN | 0 | 题库 ID |
| chapter_id | bigint unsigned | NN | 0 | 章节 ID，0=全部 |
| practice_mode | tinyint | NN | 1 | 1=顺序练习 2=随机练习 3=专项练习 4=错题重做 5=闪卡 6=斩题 |
| total_count | int unsigned | NN | 0 | 本次练习题数 |
| answered_count | int unsigned | NN | 0 | 已作答题数（断点续练进度） |
| right_count | int unsigned | NN | 0 | 答对题数 |
| wrong_count | int unsigned | NN | 0 | 答错题数 |
| correct_rate | decimal(5,2) | NN | 0 | 正确率（百分比，如 87.50） |
| duration_seconds | int unsigned | NN | 0 | 练习时长（秒） |
| status | tinyint | NN | 1 | 1=进行中 2=已完成 3=已放弃；`idx_user_practice_records_user_id_bank_id_status` |
| started_at / finished_at | datetime | 是 | NULL | 起止时间 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段；`idx_user_practice_records_user_id_created_at` |

### 1.8 user_question_notes · 用户题目笔记表

> migration: `2026_09_15_000001` ｜ 软删除：是 ｜ ⚠️ **用户私有数据，所有查询必须带 `user_id` 条件**

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | — | FK → `user_accounts.id` |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`uk_user_question_notes_user_id_question_id` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID |
| content | text | NN | — | 笔记内容 |
| is_public | tinyint | NN | 0 | 0=私有 1=公开（笔记广场，P2 功能） |
| like_count | int unsigned | NN | 0 | 点赞数 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段；`idx_user_question_notes_user_id_created_at` |

---

## 二、题库域（bank_*）

### 2.1 bank_categories · 题库分类表

> migration: `2026_09_15_000002` ｜ 软删除：是 ｜ 初始数据由 `BusinessInitSeeder` 写入（14 个一级分类）

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| parent_id | bigint unsigned | NN | 0 | 父级分类 ID，0=顶级；`idx_bank_categories_parent_id_sort_order` |
| name | varchar(32) | NN | — | 分类名称，如「建筑工程」 |
| code | varchar(32) | NN | — | 分类编码，小写下划线；`uk_bank_categories_code` |
| icon | varchar(255) | NN | '' | 分类图标 |
| level | tinyint | NN | 1 | 1=一级 2=二级 |
| question_bank_count | int unsigned | NN | 0 | 该分类下题库数（冗余，由 `data:recount` 校准） |
| sort_order | int unsigned | NN | 0 | 排序值，越小越靠前 |
| status | tinyint | NN | 1 | 1=正常 2=隐藏 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 2.2 bank_question_banks · 题库主表

> migration: `2026_09_15_000002` ｜ 软删除：是 ｜ **`user_id=0` 表示官方题库**

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | 0 | 归属用户 ID，0=官方题库；`idx_bank_question_banks_user_id_status` |
| category_id | bigint unsigned | NN | 0 | FK → `bank_categories.id`；`idx_bank_question_banks_category_id_status` |
| title | varchar(120) | NN | — | 题库名称 |
| subtitle | varchar(255) | NN | '' | 副标题 / 简介 |
| cover | varchar(255) | NN | '' | 封面图 |
| source_type | tinyint | NN | 1 | 1=用户上传 2=官方 3=购买 4=AI 生成；`idx_bank_question_banks_source_type_status` |
| charge_type | tinyint | NN | 1 | 1=免费 2=会员免费 3=单独购买 |
| price_amount | decimal(10,2) | NN | 0 | 单独购买价格（元）；`charge_type=3` 时有效 |
| question_count | int unsigned | NN | 0 | 题目数（冗余） |
| chapter_count | int unsigned | NN | 0 | 章节数（冗余） |
| practice_count | int unsigned | NN | 0 | 累计练习次数 |
| user_count | int unsigned | NN | 0 | 累计学习人数 |
| tags_json | varchar(500) | NN | '' | 标签数组 JSON |
| is_top | tinyint | NN | 0 | 0=否 1=置顶 |
| is_recommend | tinyint | NN | 0 | 0=否 1=推荐到首页；`idx_bank_question_banks_is_recommend_sort_order` |
| sort_order | int unsigned | NN | 0 | 排序值 |
| status | tinyint | NN | 1 | 1=正常 2=隐藏 3=待审核 4=已拒绝 |
| audit_remark | varchar(255) | NN | '' | 审核意见 |
| audited_by | bigint unsigned | NN | 0 | 审核人 FK → `sys_admins.id` |
| audited_at | datetime | 是 | NULL | 审核时间 |
| last_practice_at | datetime | 是 | NULL | 最近练习时间 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 2.3 bank_chapters · 题库章节表

> migration: `2026_09_15_000002` ｜ 软删除：是 ｜ 专项练习、章节分析用

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| bank_id | bigint unsigned | NN | — | FK → `bank_question_banks.id` |
| parent_id | bigint unsigned | NN | 0 | 父级章节 ID，0=顶级；`idx_bank_chapters_bank_id_parent_id` |
| name | varchar(120) | NN | — | 章节名称 |
| level | tinyint | NN | 1 | 1=章 2=节 |
| question_count | int unsigned | NN | 0 | 章节题目数（冗余） |
| sort_order | int unsigned | NN | 0 | 排序值；`idx_bank_chapters_bank_id_sort_order` |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

---

## 三、题目域（question_*）

### 3.1 question_items · 题目表

> migration: `2026_09_15_000003` ｜ 软删除：是 ｜ ⚠️ 题目量大，**列表查询禁止 `SELECT *`**，题干大文本按需取

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| bank_id | bigint unsigned | NN | — | FK → `bank_question_banks.id`；`idx_question_items_bank_id_sort_order` |
| chapter_id | bigint unsigned | NN | 0 | FK → `bank_chapters.id`，0=未分章；`idx_question_items_bank_id_chapter_id` |
| question_type | tinyint | NN | 1 | 1=单选 2=多选 3=判断 4=填空 5=简答；`idx_question_items_bank_id_question_type` |
| stem | text | NN | — | 题干（支持富文本标记） |
| stem_preview | varchar(255) | NN | '' | 题干纯文本摘要，列表与搜索用；`idx_question_items_stem_preview` |
| analysis | text | NN | — | 答案解析 |
| answer | varchar(500) | NN | '' | 正确答案。选择题存选项字母（多选如 `ABD`）；判断题存 `对`/`错`；填空简答存文本 |
| difficulty | tinyint | NN | 1 | 1=易 2=中 3=难 |
| score | decimal(5,2) | NN | 0 | 默认分值（试卷内可覆盖，见 `exam_paper_questions.score`） |
| media_json | varchar(500) | NN | '' | 题干媒体资源 JSON（图片 / 音频） |
| source_type | tinyint | NN | 1 | 1=手动录入 2=文档导入 3=拍照 OCR 4=AI 生成 |
| status | tinyint | NN | 1 | 1=正常 2=隐藏 3=待审核 |
| answer_count | int unsigned | NN | 0 | 累计作答次数（冗余） |
| right_count | int unsigned | NN | 0 | 累计答对次数（冗余） |
| correct_rate | decimal(5,2) | NN | 0 | 正确率（百分比，冗余） |
| sort_order | int unsigned | NN | 0 | 题库内题号排序 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

**与 `question_options` 的约定**：选择题（`question_type` 1/2/3）必须至少 2 个选项；
非选择题（4/5）不写 `question_options`，答案只存 `answer` 字段。
`question_options.is_correct` 与 `question_items.answer` 必须保持一致（由 Service 层在同一事务内写入）。

### 3.2 question_options · 题项（选项）表

> migration: `2026_09_15_000003` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`idx_question_options_question_id_sort_order` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID，便于随题库批量删除；`idx_question_options_bank_id` |
| option_key | varchar(2) | NN | — | 选项序号 `A`~`H` |
| content | varchar(1000) | NN | — | 选项内容 |
| is_correct | tinyint | NN | 0 | 0=否 1=是 |
| media_json | varchar(500) | NN | '' | 选项媒体资源 JSON |
| sort_order | int unsigned | NN | 0 | 排序值 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 3.3 question_reports · 试题报错表

> migration: `2026_09_15_000003` ｜ 软删除：是 ｜ 总后台审核队列之一

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`idx_question_reports_question_id_status` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID |
| user_id | bigint unsigned | NN | 0 | 反馈用户 ID；`idx_question_reports_user_id` |
| report_type | tinyint | NN | 1 | 1=答案错误 2=解析错误 3=题干错误 4=图片错误 5=题目重复 9=其他 |
| content | varchar(500) | NN | '' | 补充说明 |
| images_json | varchar(1000) | NN | '' | 截图数组 JSON（object_key 列表） |
| status | tinyint | NN | 1 | 1=待处理 2=已处理 3=已忽略；`idx_question_reports_status_created_at` |
| handled_by | bigint unsigned | NN | 0 | 处理人 FK → `sys_admins.id` |
| handle_remark | varchar(255) | NN | '' | 处理意见 |
| handled_at | datetime | 是 | NULL | 处理时间 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 3.4 question_import_tasks · 导题解析任务表

> migration: `2026_09_15_000003` ｜ 软删除：是 ｜ AI 导题 / 文档解析的进度载体

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| task_no | varchar(32) | NN | — | 任务编号 `IMP{yyyyMMdd}{6位序列}`；`uk_question_import_tasks_task_no` |
| user_id | bigint unsigned | NN | — | 发起用户 ID；`idx_question_import_tasks_user_id_status` |
| bank_id | bigint unsigned | NN | 0 | 目标题库 ID，0=解析完成后新建；`idx_question_import_tasks_bank_id_status` |
| file_id | bigint unsigned | NN | 0 | FK → `file_assets.id` |
| origin_name | varchar(255) | NN | '' | 用户上传的原始文件名（仅留档） |
| file_ext | varchar(16) | NN | '' | 源文件扩展名 |
| import_mode | tinyint | NN | 1 | 1=文档导入 2=手动录入 3=拍照 OCR 4=试题答案分离 |
| total_count | int unsigned | NN | 0 | 解析出题目总数 |
| success_count | int unsigned | NN | 0 | 解析成功题数 |
| fail_count | int unsigned | NN | 0 | 解析失败题数 |
| progress | tinyint | NN | 0 | 进度 0~100 |
| status | tinyint | NN | 1 | 1=待解析 2=解析中 3=待校对 4=已完成 5=失败 |
| error_message | varchar(500) | NN | '' | 失败原因 |
| result_json | longtext | 是 | NULL | 解析结果（待校对数据），校对完成后可清理以省空间 |
| started_at / finished_at | datetime | 是 | NULL | 起止时间 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

---

## 四、考试域（exam_*）

### 4.1 exam_papers · 试卷表

> migration: `2026_09_15_000004` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| paper_no | varchar(32) | NN | — | 试卷编号 `EP{yyyyMMdd}{6位序列}`；`uk_exam_papers_paper_no` |
| user_id | bigint unsigned | NN | — | 所属用户 ID；`idx_exam_papers_user_id_created_at` |
| bank_id | bigint unsigned | NN | 0 | 来源题库 ID，0=多题库组卷；`idx_exam_papers_bank_id_paper_type` |
| title | varchar(120) | NN | — | 试卷标题 |
| paper_type | tinyint | NN | 1 | 1=模拟考试 2=自测 3=错题卷 4=随机组卷 |
| generate_mode | tinyint | NN | 1 | 1=随机抽题 2=按章节 3=按题型 4=手动选题 |
| question_count | int unsigned | NN | 0 | 题目数 |
| total_score | decimal(8,2) | NN | 0 | 试卷总分 |
| pass_score | decimal(8,2) | NN | 0 | 及格分 |
| duration_minutes | int unsigned | NN | 0 | 考试时长（分钟），0=不限时 |
| config_json | varchar(1000) | NN | '' | 组卷规则 JSON（章节分布、题型分布） |
| status | tinyint | NN | 1 | 1=正常 2=已作废 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 4.2 exam_paper_questions · 试卷题目关联表

> migration: `2026_09_15_000004` ｜ 软删除：是 ｜ 组卷完成后此表即冻结，保证历史试卷可复现

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| paper_id | bigint unsigned | NN | — | FK → `exam_papers.id`；`uk_exam_paper_questions_paper_id_question_id` |
| question_id | bigint unsigned | NN | — | FK → `question_items.id`；`idx_exam_paper_questions_question_id` |
| bank_id | bigint unsigned | NN | 0 | 题目所属题库 ID |
| sort_order | int unsigned | NN | 0 | 试卷内题号顺序；`idx_exam_paper_questions_paper_id_sort_order` |
| score | decimal(5,2) | NN | 0 | **本卷内**该题分值（覆盖 `question_items.score`） |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 4.3 exam_records · 考试记录表

> migration: `2026_09_15_000004` ｜ 软删除：**否**（流水表）

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| record_no | varchar(32) | NN | — | 记录编号 `ER{yyyyMMdd}{6位序列}`；`uk_exam_records_record_no`，**交卷幂等键** |
| user_id | bigint unsigned | NN | — | 用户 ID；`idx_exam_records_user_id_created_at` |
| paper_id | bigint unsigned | NN | — | FK → `exam_papers.id`；`idx_exam_records_paper_id` |
| bank_id | bigint unsigned | NN | 0 | 冗余题库 ID；`idx_exam_records_user_id_bank_id` |
| paper_title | varchar(120) | NN | '' | 试卷标题（冗余，列表免联表） |
| total_count | int unsigned | NN | 0 | 总题数 |
| answered_count | int unsigned | NN | 0 | 已作答题数 |
| right_count | int unsigned | NN | 0 | 答对题数 |
| wrong_count | int unsigned | NN | 0 | 答错题数 |
| unanswer_count | int unsigned | NN | 0 | 未作答题数 |
| total_score | decimal(8,2) | NN | 0 | 试卷总分 |
| get_score | decimal(8,2) | NN | 0 | 实得分 |
| correct_rate | decimal(5,2) | NN | 0 | 正确率（百分比） |
| duration_seconds | int unsigned | NN | 0 | 作答耗时（秒） |
| is_passed | tinyint | NN | 0 | 0=未及格 1=及格 |
| status | tinyint | NN | 1 | 1=进行中 2=已交卷 3=超时自动交卷 4=已作废 |
| started_at / submitted_at | datetime | 是 | NULL | 开始 / 交卷时间 |
| created_at / updated_at | datetime | 是 | NULL | 无 `deleted_at` |

### 4.4 exam_answers · 考试作答明细表

> migration: `2026_09_15_000004` ｜ 软删除：**否**（流水表）

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| record_id | bigint unsigned | NN | — | FK → `exam_records.id`；`uk_exam_answers_record_id_question_id` |
| paper_id | bigint unsigned | NN | 0 | 试卷 ID |
| user_id | bigint unsigned | NN | — | 用户 ID；`idx_exam_answers_user_id_question_id` |
| question_id | bigint unsigned | NN | — | FK → `question_items.id` |
| user_answer | varchar(500) | NN | '' | 用户作答内容（格式同 `question_items.answer`） |
| is_correct | tinyint | NN | 0 | 0=否 1=是 |
| score | decimal(5,2) | NN | 0 | 本题得分 |
| duration_seconds | int unsigned | NN | 0 | 本题作答耗时（秒） |
| created_at / updated_at | datetime | 是 | NULL | 无 `deleted_at` |

---

## 五、交易域（order_*）

### 5.1 order_member_plans · 会员套餐表

> migration: `2026_09_15_000005` ｜ 软删除：是 ｜ 套餐变更只影响新订单，历史订单存快照

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| name | varchar(64) | NN | — | 套餐名称 |
| level | tinyint | NN | 1 | 对应会员等级 1=月卡 2=季卡 3=年卡 4=永久 |
| duration_days | int unsigned | NN | 30 | 有效天数，永久为 0 |
| price_amount | decimal(10,2) | NN | 0 | 现价（元） |
| origin_amount | decimal(10,2) | NN | 0 | 原价（元），用于划线价 |
| description | varchar(255) | NN | '' | 套餐说明 |
| benefits_json | varchar(1000) | NN | '' | 权益列表 JSON |
| ai_import_quota | int unsigned | NN | 0 | 赠送 AI 导题配额（份） |
| is_recommend | tinyint | NN | 0 | 0=否 1=推荐 |
| sort_order | int unsigned | NN | 0 | 排序值；`idx_order_member_plans_status_sort_order` |
| status | tinyint | NN | 1 | 1=上架 2=下架 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 5.2 order_orders · 订单表

> migration: `2026_09_15_000005` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| order_no | varchar(32) | NN | — | 订单号 `OD{yyyyMMddHHmmss}{6位随机}`；`uk_order_orders_order_no` |
| user_id | bigint unsigned | NN | — | 下单用户 ID；`idx_order_orders_user_id_status` |
| order_type | tinyint | NN | 1 | 1=会员 2=题库购买 3=学习资料购买 |
| biz_id | bigint unsigned | NN | 0 | 商品 ID（套餐 / 题库 / 资料） |
| biz_title | varchar(120) | NN | '' | 商品名称快照（防商品改名影响历史订单） |
| origin_amount | decimal(10,2) | NN | 0 | 订单原价（元） |
| discount_amount | decimal(10,2) | NN | 0 | 优惠金额（元） |
| pay_amount | decimal(10,2) | NN | 0 | 应付金额（元） |
| pay_channel | tinyint | NN | 1 | 1=微信支付 2=支付宝 |
| status | tinyint | NN | 0 | 0=待支付 1=已支付 2=已取消 3=已退款 4=已关闭；`idx_order_orders_status_created_at` |
| client_platform | varchar(20) | NN | '' | 下单端 `mp-weixin` / `app-android` / `h5` / `console` |
| remark | varchar(255) | NN | '' | 备注 |
| expired_at | datetime | 是 | NULL | 支付超时时间，超时自动关闭 |
| paid_at / cancelled_at / refunded_at | datetime | 是 | NULL | 状态时间戳 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段；`idx_order_orders_user_id_created_at` |

### 5.3 order_payments · 支付流水表

> migration: `2026_09_15_000005` ｜ 软删除：**否**（流水表，对账凭证）

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| order_id | bigint unsigned | NN | — | FK → `order_orders.id`；`idx_order_payments_order_id` |
| order_no | varchar(32) | NN | — | 订单号（冗余，对账用）；`idx_order_payments_order_no` |
| user_id | bigint unsigned | NN | — | 用户 ID |
| transaction_no | varchar(64) | NN | '' | 第三方支付流水号；`idx_order_payments_transaction_no` |
| pay_channel | tinyint | NN | 1 | 1=微信支付 2=支付宝 |
| pay_amount | decimal(10,2) | NN | 0 | 实付金额（元） |
| status | tinyint | NN | 1 | 1=待支付 2=成功 3=失败 4=已退款；`idx_order_payments_status_created_at` |
| notify_count | tinyint unsigned | NN | 0 | 回调通知次数（排查重复回调） |
| notify_json | longtext | 是 | NULL | 回调原文（对账留档，含敏感信息，仅总后台可查） |
| fail_reason | varchar(255) | NN | '' | 失败原因 |
| paid_at | datetime | 是 | NULL | 支付成功时间 |
| created_at / updated_at | datetime | 是 | NULL | 无 `deleted_at` |

---

## 六、文件域（file_*）

### 6.1 file_categories · 文件分类表

> migration: `2026_09_15_000006` ｜ 软删除：是 ｜ `user_id=0` 为系统预置分类

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | 0 | 归属用户 ID，0=系统预置 |
| parent_id | bigint unsigned | NN | 0 | 父级分类 ID，0=顶级；`idx_file_categories_user_id_parent_id_sort_order` |
| name | varchar(64) | NN | — | 分类名称 |
| code | varchar(32) | NN | — | 分类编码；`uk_file_categories_user_id_code`（同一用户下唯一） |
| icon | varchar(255) | NN | '' | 分类图标 |
| file_count | int unsigned | NN | 0 | 分类下文件数（冗余） |
| sort_order | int unsigned | NN | 0 | 排序值 |
| status | tinyint | NN | 1 | 1=正常 2=隐藏 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 6.2 file_assets · 文件资源表（OSS 登记，唯一入口）

> migration: `2026_09_15_000006` ｜ 软删除：是 ｜ 详见 `docs/05-OSS存储与文件分类规范.md`
> ⚠️ **所有上传文件必须落此表**，不允许存在「数据库无记录但 OSS 里有对象」的孤儿文件

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| user_id | bigint unsigned | NN | 0 | 归属用户 ID，0=系统文件；`idx_file_assets_user_id_biz_type` |
| biz_type | tinyint | NN | 1 | 1=题库源文件 2=题目图片 3=学习资料 4=课程音视频 5=头像 6=公开静态 9=临时文件 |
| bank_id | bigint unsigned | NN | 0 | 归属题库 ID；`idx_file_assets_bank_id_category_id` |
| category_id | bigint unsigned | NN | 0 | 归属文件分类 ID，0=未分类 |
| question_id | bigint unsigned | NN | 0 | 归属题目 ID，0=不属于题目 |
| origin_name | varchar(255) | NN | '' | 用户原始文件名（仅留档展示，**不作为存储路径**） |
| object_key | varchar(500) | NN | — | 存储对象 Key；`uk_file_assets_object_key` |
| file_ext | varchar(16) | NN | '' | 扩展名（小写，不含点） |
| file_size | bigint unsigned | NN | 0 | 文件大小（字节） |
| file_hash | varchar(64) | NN | '' | 文件 MD5，秒传与去重；`idx_file_assets_file_hash` |
| mime_type | varchar(100) | NN | '' | MIME 类型 |
| storage | varchar(20) | NN | 'qiniu' | 存储方 `qiniu` / `aliyun` / `local` |
| is_public | tinyint | NN | 0 | 0=私有（签名 URL） 1=公开读 |
| status | tinyint | NN | 1 | 1=待上传 2=已上传 3=解析中 4=已归档 5=失败；`idx_file_assets_status_expired_at` |
| ref_count | int unsigned | NN | 0 | 引用计数，为 0 且软删才可清理 OSS 对象 |
| expired_at | datetime | 是 | NULL | 临时文件过期时间，定时任务清理 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

---

## 七、内容 / 运营域（content_*）

### 7.1 content_banners · 运营位表

> migration: `2026_09_15_000007` ｜ 软删除：是 ｜ 客户端需读取，故不用 `sys_` 前缀

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| position_code | varchar(32) | NN | — | 投放位置 `home_top`=首页轮播 `home_recommend`=首页推荐 `mine_entry`=我的页入口；`idx_content_banners_position_code_status_sort_order` |
| title | varchar(64) | NN | '' | 标题 |
| subtitle | varchar(120) | NN | '' | 副标题 |
| image | varchar(255) | NN | — | 图片地址（公开空间 URL 或 object_key） |
| link_type | tinyint | NN | 1 | 1=不跳转 2=题库 3=学习资料 4=外链 5=活动页 |
| link_value | varchar(255) | NN | '' | 跳转目标值（题库 ID / 资料 ID / URL） |
| client_scope | varchar(32) | NN | 'all' | 投放端 `all` / `mp-weixin` / `app-android` / `h5` |
| click_count | int unsigned | NN | 0 | 点击次数 |
| sort_order | int unsigned | NN | 0 | 排序值 |
| status | tinyint | NN | 1 | 1=启用 2=停用 |
| started_at / ended_at | datetime | 是 | NULL | 投放时间段 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

---

## 八、系统域（sys_*）— 仅总后台可访问

> ⚠️ 本节所有表对 `app_client` / `app_console` 数据库账号**无任何权限**（见 `docs/06` §二、`server/database/sql/01_create_users_and_privileges.sql`）

### 8.1 sys_admins · 系统管理员表

> migration: `2026_09_15_000008` ｜ 软删除：是 ｜ 独立账号体系，与客户端用户完全不互通

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| username | varchar(32) | NN | — | 登录账号；`uk_sys_admins_username` |
| password | varchar(100) | NN | — | bcrypt 哈希（禁止明文） |
| real_name | varchar(32) | NN | '' | 真实姓名 |
| mobile | varchar(20) | NN | '' | 手机号 |
| email | varchar(64) | NN | '' | 邮箱 |
| avatar | varchar(255) | NN | '' | 头像 |
| is_super | tinyint | NN | 0 | 0=否 1=超级管理员（拥有全部权限，不可删除） |
| status | tinyint | NN | 1 | 1=正常 2=禁用；`idx_sys_admins_status` |
| login_fail_count | tinyint unsigned | NN | 0 | 连续登录失败次数，达 5 次锁定 |
| locked_until | datetime | 是 | NULL | 锁定截止时间 |
| last_login_at | datetime | 是 | NULL | 最后登录时间 |
| last_login_ip | varchar(45) | NN | '' | 最后登录 IP |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.2 sys_roles · 角色表

> migration: `2026_09_15_000008` ｜ 软删除：是 ｜ 预置 4 个角色：`super_admin` / `operation` / `customer_service` / `finance`

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| name | varchar(32) | NN | — | 角色名称 |
| code | varchar(32) | NN | — | 角色编码；`uk_sys_roles_code` |
| description | varchar(255) | NN | '' | 角色描述 |
| is_system | tinyint | NN | 0 | 0=否 1=系统预置（不可删除） |
| sort_order | int unsigned | NN | 0 | 排序值 |
| status | tinyint | NN | 1 | 1=正常 2=停用 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.3 sys_permissions · 权限点表

> migration: `2026_09_15_000008` ｜ 软删除：是 ｜ 权限编码格式 `模块:资源:动作`

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| parent_id | bigint unsigned | NN | 0 | 父级权限 ID，0=顶级菜单；`idx_sys_permissions_parent_id_sort_order` |
| name | varchar(64) | NN | — | 权限名称 |
| code | varchar(64) | NN | — | 权限编码，如 `sys:config:update`；`uk_sys_permissions_code` |
| type | tinyint | NN | 1 | 1=菜单 2=按钮 3=数据 |
| route_path | varchar(128) | NN | '' | 前端路由路径 |
| icon | varchar(64) | NN | '' | 菜单图标 |
| sort_order | int unsigned | NN | 0 | 排序值 |
| status | tinyint | NN | 1 | 1=正常 2=停用 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.4 sys_role_permissions · 角色-权限关联表

> migration: `2026_09_15_000008` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| role_id | bigint unsigned | NN | — | FK → `sys_roles.id`；`uk_sys_role_permissions_role_id_permission_id` |
| permission_id | bigint unsigned | NN | — | FK → `sys_permissions.id`；`idx_sys_role_permissions_permission_id` |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.5 sys_admin_roles · 管理员-角色关联表

> migration: `2026_09_15_000008` ｜ 软删除：是

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| admin_id | bigint unsigned | NN | — | FK → `sys_admins.id`；`uk_sys_admin_roles_admin_id_role_id` |
| role_id | bigint unsigned | NN | — | FK → `sys_roles.id`；`idx_sys_admin_roles_role_id` |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.5.1 sys_configs · 系统配置表（配置中心）

> migration: `2026_09_15_000008` ｜ 软删除：是 ｜ **短信 / 微信 / OSS / 支付 / AI / OCR 全部 KEY 存于此表**，界面可视化修改

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| group_code | varchar(32) | NN | — | 分组 `sms` / `wechat` / `payment` / `storage` / `ai` / `ocr` / `site` |
| config_key | varchar(64) | NN | — | 配置键，点号命名，如 `storage.access_key`；`uk_sys_configs_group_code_config_key` |
| config_value | text | 是 | NULL | 配置值；`is_secret=1` 时为 AES-256-GCM 加密串 |
| default_value | varchar(255) | NN | '' | 默认值（非密文），用于恢复 |
| value_type | tinyint | NN | 1 | 1=字符串 2=数字 3=布尔 4=JSON 5=密文 |
| is_secret | tinyint | NN | 0 | 0=否 1=是（界面脱敏显示为 `sk_****f3a1`） |
| title | varchar(64) | NN | '' | 配置项中文名 |
| remark | varchar(255) | NN | '' | 填写说明 / 获取途径 |
| is_system | tinyint | NN | 0 | 0=否 1=系统内置（不可删除） |
| sort_order | int unsigned | NN | 0 | 排序值；`idx_sys_configs_group_code_sort_order` |
| status | tinyint | NN | 1 | 1=启用 2=停用 |
| created_at / updated_at / deleted_at | datetime | 是 | NULL | 通用四字段 |

### 8.6 sys_logs · 操作日志表

> migration: `2026_09_15_000008` ｜ 软删除：**否**（流水表）｜ 所有写操作强制留痕

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| admin_id | bigint unsigned | NN | 0 | 操作人 ID；`idx_sys_logs_admin_id_created_at` |
| admin_name | varchar(32) | NN | '' | 操作人账号（冗余留档） |
| module | varchar(32) | NN | '' | 模块，如 `config` / `bank` / `user`；`idx_sys_logs_module_action_created_at` |
| action | varchar(32) | NN | '' | 动作 `create` / `update` / `delete` / `audit` |
| description | varchar(255) | NN | '' | 操作描述 |
| target_type | varchar(64) | NN | '' | 目标对象类型；`idx_sys_logs_target_type_target_id` |
| target_id | bigint unsigned | NN | 0 | 目标对象 ID |
| before_json | longtext | 是 | NULL | 变更前快照 |
| after_json | longtext | 是 | NULL | 变更后快照 |
| ip | varchar(45) | NN | '' | 操作 IP |
| user_agent | varchar(255) | NN | '' | User-Agent |
| request_id | varchar(64) | NN | '' | 链路追踪 ID |
| result | tinyint | NN | 1 | 1=成功 2=失败 |
| created_at | datetime | 是 | NULL | 仅创建时间 |

### 8.7 sys_login_logs · 管理员登录日志表

> migration: `2026_09_15_000008` ｜ 软删除：**否**（流水表）

| 字段 | 类型 | 空 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| id | bigint unsigned | NN | 自增 | 主键 |
| admin_id | bigint unsigned | NN | 0 | 管理员 ID，失败且账号不存在时为 0；`idx_sys_login_logs_admin_id_created_at` |
| username | varchar(32) | NN | '' | 登录账号；`idx_sys_login_logs_username_created_at` |
| login_type | tinyint | NN | 1 | 1=账号密码 2=TOTP 二次验证 |
| ip | varchar(45) | NN | '' | 登录 IP；`idx_sys_login_logs_ip_created_at` |
| user_agent | varchar(255) | NN | '' | User-Agent |
| status | tinyint | NN | 1 | 1=成功 2=失败 |
| message | varchar(255) | NN | '' | 失败原因 / 备注 |
| created_at | datetime | 是 | NULL | 仅创建时间 |

---

## 九、登记统计

| 域 | 表数 | 已登记 |
| --- | --- | --- |
| 用户域 | 8 | ✅ |
| 题库域 | 3 | ✅ |
| 题目域 | 4 | ✅ |
| 考试域 | 4 | ✅ |
| 交易域 | 3 | ✅ |
| 文件域 | 2 | ✅ |
| 内容域 | 1 | ✅ |
| 系统域 | 8 | ✅ |
| **合计** | **33** | **33 / 33** |
